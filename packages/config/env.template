# Database Engine (mongo or postgres)
DB_ENGINE=mongo

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/enterprise

# PostgreSQL Configuration
POSTGRES_URI=postgresql://user:password@localhost:5432/enterprise

# JWT Secret Key
JWT_SECRET=your_jwt_secret_key_here

# JWT Token Expiry
JWT_EXPIRY=1h
